context('A - From homepage to map page', () => {
  beforeEach(() => {
    cy.viewport(1280, 720);
  });

  describe('homepage', () => {
    it('when visit page / must shown...', () => {
      expect(true).toBe(true);
    });
  });
});
